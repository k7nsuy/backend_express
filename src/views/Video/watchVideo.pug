extend ../base.pug

block content 
    video(src="/" + video.fileUrl controls)
    div 
        button#play Play 
        button#mute Mute 
        span#time 00:00 / 00:00 
        input(type="range", step="0.1" min=0, max=1)#volume
    //- br
    //- br
    //- div
    //-     p=video.description
    //-     br
    //-     small=video.createdAt
    //-     br
    //-     br
    //-     small 
    //-         a(href=`/users/${video.owner._id}`) Uploaded by #{video.owner.name}
    //-     br
    //- if String(video.owner._id) === String(profile._id)
    //-     a(href=`${video.id}/edit`) Edit Video ➡️
    //-     br
    //-     a(href=`${video.id}/delete`) Delete This Video ❌   

block scripts 
    script(src="/assets/js/videoPlayer.js") 